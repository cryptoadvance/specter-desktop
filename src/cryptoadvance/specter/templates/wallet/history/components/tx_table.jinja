{# 
    tx_table - Empty table component
    Parameters:
    - isEmpty: Boolean, true if should show an item indicating empty list, false if should show the list contents.
    - content: The content of the table (its rows).
    - specter: Specter object

 #}
{% macro tx_table(isEmpty, content, specter) -%}
    <table>
        <thead>
            <tr>
                <th></th>
                <th class="optional">TxID</th>
                <th class="optional">Address</th>
                <th>Amount</th>
                <th>Status</th>
		{% if specter.config.validate_merkle_proofs %}
		  <th>
		      <div class="tool-tip">
			  <i class="tool-tip__icon">i</i>
			  <p class="tool-tip__info">
			      <span class="info">
				  <span class="info__title">Merkle Proof Validation<br><br></span>
				  Specter-Desktop validates <a href="https://github.com/bitcoin/bips/blob/master/bip-0037.mediawiki">BIP 37</a> Merkle Proofs from your full node <strong>guaranteeing</strong> that if we display a transaction as confirmed then it has been included in this block hash.<br><br>
				  However, if your full node was compromised it could try to trick you with a fake block that was never mined into bitcoin's blockchain!<br><br>
				  If you find this block hash in other locations (other nodes, block explorers, etc), then you can be sure this transaction exists in their blockchain as well.<br><br>

				  You can turn off this feature <a href="/settings/general">here</a>.
			      </span>
			  </p>
		      </div>
		      Block Hash
		  </th>
	        {% endif %}
            </tr>
	</thead>
	<tbody>
	    {% if not isEmpty %}
                {{ content }}
            {% else %}
                <td></td>
                <td>No transactions yet.</td>
                <td class="optional"></td>
                <td class="optional"></td>
		{% if specter.config.validate_merkle_proofs %}
		  <td class="optional"></td>
		{% endif %}
                <td></td>
	    {% endif %}
	</tbody>
    </table>
{%- endmacro %}
